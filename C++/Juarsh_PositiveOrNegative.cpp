#include<iostream>

using namespace std;

int main()
{
  int num;
  cout<<"Enter a number:";
  cin>>num;
  if(num>0)
  cout<<"Number is Positive";
  
  if(num<0)
  cout<<"Number is Negative";
  
  if(num==0)
  cout<<"Number is zero";
  
  return 0;
}
