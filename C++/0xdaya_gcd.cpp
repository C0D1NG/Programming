#include <bits/stdc++.h>
using namespace std;
int main() {
	long long a, b;
	cout << "Enter two numbers: ";
	cin >> a >> b;
	cout << "gcd of " << a << " and " << b << " is " << __gcd(a, b);
}
